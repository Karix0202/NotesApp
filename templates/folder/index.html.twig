{% extends 'base.html.twig' %}

{% block title %}Folders | NotesApp{% endblock %}

{% block container %}
    <div class="row row-cols-1 row-cols-md-3 g-4">
        {% for folder in folders %}
            <div class="col">
                {{ include('folder/folder_card.html.twig') }}
            </div>
        {% endfor %}
    </div>
{% endblock %}